/**
 * Package name Android usado para fallback de abertura da página na Play Store.
 * Ajuste este valor se o `expo.android.package` (app.json) mudar.
 */
export const ANDROID_PACKAGE_NAME = 'com.algoritmum.msgt';
